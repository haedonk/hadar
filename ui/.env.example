# API base URL — baked in at build time by Vite.
# For local dev pointing at the cluster: http://<node-ip>:30800
# For local dev with api/ running locally: http://localhost:8000
VITE_API_URL=http://localhost:8000
